<Window x:Class="WPFUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFUI"
        mc:Ignorable="d" FontSize="16"
        Title="RotationTracker" Height="500" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Margin="15 15 15 5" Text="Employees:"></TextBlock>
        <ListBox Grid.Column="0" Grid.Row="1" Name="employeeListBox" Margin="15 0 15 15">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button x:Name="editEmployeesButton" Grid.Row="2" Grid.Column="0" Width="45" Padding="5" Click="EditEmployeesButton_Click">Edit</Button>

        <TextBlock x:Name="rotation1Name" Grid.Column="1" Grid.Row="0" Margin="15 15 15 5" Text="Rotation 1:"></TextBlock>
        <ListBox Grid.Column="1" Grid.Row="1" Name="rotation1ListBox" Margin="15 0 15 15">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button x:Name="editRotation1Button" Grid.Row="2" Grid.Column="1" Width="45" Padding="5" Click="EditRotation1Button_Click">Edit</Button>

    </Grid>
</Window>
